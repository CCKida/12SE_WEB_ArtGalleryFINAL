Make sure the instances are bellow everything else or the data will be unseen. When instances are above all the file will automatically create a new instance file (datafile)
for the log in. 

If you use other instances you are unable to use your login. There is only one system.
